'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up (queryInterface, Sequelize) {
    //await queryInterface.addColumn("reqSkills", "typeId", { type: Sequelize.INTEGER });
    //await queryInterface.addColumn("reqSkills", "type", { type: Sequelize.STRING });
  },

  async down (queryInterface, Sequelize) {
  
  }
};
